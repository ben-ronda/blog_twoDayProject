<div id = "post">
  <h2>{{post.title}}</h2>
  By <h4>{{post.author}}</h4>
  <hr>
  <p>
    {{post.content}}
  </p>
  <img src="{{post.image}}">
  <button class="btn btn-danger"{{action 'delete' post}}>Delete</button>

</div>
