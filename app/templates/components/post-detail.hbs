<div id = "post">
  <h2>{{post.title}}</h2>
  By <h4>{{post.author}}</h4>
  <hr>
  <p>
    {{post.content}}
  </p>
  <img src="{{post.image}}">
  <button {{action 'delete' post}}>Delete</button>

</div>
